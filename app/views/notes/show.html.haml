.row
  .col-md-12
    .page-header
      %h1
        = "#{@note.noted_at}"
        %small
          = link_to "#{@note.body.nickname}さん", body_path(@note.body)
          のメモ
.row
  .col-md-6
    %img.img-responsive{src: 'http://placehold.it/700x400', alt: ''}
  .col-md-6
    = simple_format @note.detail
    = link_to '編集', edit_note_path(@note), class: 'btn btn-primary'

