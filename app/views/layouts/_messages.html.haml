- bootstrap_key_conversion_map = {notice: :info, alert: :danger}.with_indifferent_access
- flash.each do |key, value|
  - key = bootstrap_key_conversion_map[key] || key
  %div{class: "alert alert-#{key}"}
    %a{href: '#', 'data-dismiss': 'alert', class: 'close'} ×
    %ul
      %li= value
